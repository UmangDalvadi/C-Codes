#include <stdio.h>
void max(int , int );


int main()
{
    int a, b;
    scanf("%d%d", &a, &b);
    max(a, b);

    return 0;
}

void max(int a,int b)
{
    if (a > b)
    {
        printf("%d is max", a);
    }
    else
    {
        printf("ans: %d is maximum\n", b);
    }
 
     printf("\nName: Umang Dalvadi");
    printf("\nEnrollment no:210210116020");

}
